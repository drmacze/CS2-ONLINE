# Local vendor directory

Put owner-supplied `.mcaddon` / `.mcpack` dependencies here. Binary vendor files are ignored by Git.

The build is intentionally dependency-selective. No CSO terrorist/NPC, Heist, Flashlight, Tactical HUD/minimap or block-destruction packs are imported.
